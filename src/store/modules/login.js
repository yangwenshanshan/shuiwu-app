const state = {
  loginStatus: 0,
}

const actions = {
}

const mutations = {
  setLoginStatus (state, status) {
    state.loginStatus = status
  },
}

export default {
  state,
  actions,
  mutations
}